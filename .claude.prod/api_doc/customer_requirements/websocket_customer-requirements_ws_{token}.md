# WEBSOCKET /customer-requirements/ws/{token}

WebSocket /customer-requirements/ws/{token}. Requires authentication.

**Response:**
Success response.

**Errors:**
- `401` — Not authenticated
- `404` — Not found
