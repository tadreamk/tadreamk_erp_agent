# DELETE /renewal-contract-workflow/{workflow_id}

Delete Workflow. Public endpoint (no auth).

**Path Parameters:**
| Parameter | Type | Description |
|-----------|------|-------------|
| workflow_id | string |  |

**Response:**
No content (`204`).

**Errors:**
- `422` — Validation Error
