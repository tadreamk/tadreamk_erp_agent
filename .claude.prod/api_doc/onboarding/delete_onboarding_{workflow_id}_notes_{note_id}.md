# DELETE /onboarding/{workflow_id}/notes/{note_id}

Delete Note. Public endpoint (no auth).

**Path Parameters:**
| Parameter | Type | Description |
|-----------|------|-------------|
| workflow_id | string |  |
| note_id | string |  |

**Response:**
No content (`204`).

**Errors:**
- `422` — Validation Error
