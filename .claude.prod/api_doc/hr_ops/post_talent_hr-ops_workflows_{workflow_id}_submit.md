# POST /talent/hr-ops/workflows/{workflow_id}/submit

Submit My Workflow. Requires authentication.

**Path Parameters:**
| Parameter | Type | Description |
|-----------|------|-------------|
| workflow_id | string |  |

**Request Body:**
JSON body (`object`).

**Response:**
```json
{}
```

**Errors:**
- `422` — Validation Error
