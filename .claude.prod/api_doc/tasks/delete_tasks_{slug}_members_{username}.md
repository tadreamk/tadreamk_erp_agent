# DELETE /tasks/{slug}/members/{username}

Remove Member From Task. Public endpoint (no auth).

**Path Parameters:**
| Parameter | Type | Description |
|-----------|------|-------------|
| slug | string |  |
| username | string |  |

**Response:**
```json
{}
```

**Errors:**
- `422` — Validation Error
