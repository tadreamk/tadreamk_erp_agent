# GET /tasks/{slug}

Get Task. Public endpoint (no auth).

**Path Parameters:**
| Parameter | Type | Description |
|-----------|------|-------------|
| slug | string |  |

**Response:**
```json
{}
```

**Errors:**
- `422` — Validation Error
